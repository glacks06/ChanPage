---
title: "1773. 폭죽쇼 풀이"
description: ""
category: "bruteforce"
date: 2026-03-22
---

[https://www.acmicpc.net/problem/1773](https://www.acmicpc.net/problem/1773)

0부터 c까지 시간이 흐르는 동안(반복문)
시간이 각 학생의 폭죽 주기와 나누었을 때 나머지가 0이면 결과값(cnt)에 1을 더한 후 마지막에 결과값을 출력한다.
(이때 나머지가 0인것은 폭죽이 터질 시점임을 의미함)
(주의: 결과값에 1을 더한 후 다른 학생의 폭죽과 중복되어서 카운팅이 되지 않도록 해야함)

C

```c
#include <stdio.h>

int main(){
    int n, c; 
    if (scanf("%d %d", &n, &c) != 2) return 0;
    
    int a[100]; // 학생 수는 최대 100명
    for(int i = 0; i < n; i++) scanf("%d", &a[i]);
    
    int cnt = 0;
    for(int i = 1; i <= c; i++){
        for(int j = 0; j < n; j++){
            if(i % a[j] == 0) {
                cnt++; 
                break; // 한 명이라도 터뜨리면 해당 초는 카운트하고 다음 초로 넘어감 (중복 방지)
            }
        }
    }
    printf("%d", cnt);
    return 0;
}
```

<br>

Python

```python
import sys

# 입력 속도를 위해 sys.stdin.read 사용 가능
input_data = sys.stdin.read().split()
if not input_data:
    exit()

n = int(input_data[0])
c = int(input_data[1])
periods = [int(x) for x in input_data[2:]]

cnt = 0
# 1초부터 종료 시간 c초까지 검사
for i in range(1, c + 1):
    for p in periods:
        if i % p == 0:
            cnt += 1
            break # 중복 카운팅 방지
            
print(cnt)
```

중복을 피하기 위해 `break`를 사용하는 로직이 아주 깔끔하네요\! 다만, 이 문제는 시간 제한과 $C$의 범위($2,000,000$)에 따라 **에라토스테네스의 체**와 유사한 방식을 사용하면 더 빠르게 풀 수도 있습니다. (미리 $C$ 크기의 불리언 배열을 만들어 두고 각 주기의 배수 자리를 체크하는 방식이죠.)

다음 문제로는 조금 더 복잡한 구현이 필요한 **[2108번: 통계학](https://www.acmicpc.net/problem/2108)** 문제를 풀어보시는 건 어떨까요? 산술평균, 중앙값, 최빈값, 범위를 구하는 문제라 연습하기 좋습니다. 원하신다면 가이드를 드릴 수 있어요\!